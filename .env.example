# Secrets pour docker-compose (copier en ".env" à côté de docker-compose.yml).
# Le fichier .env réel n'est PAS versionné (voir .gitignore).
GROQ_API_KEY=ta_cle_groq_ici
DB_PASSWORD=turbomaint_secret
